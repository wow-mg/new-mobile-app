# Kakao DB Persistence — API Contract Plan

- Work unit: `room986-kakao-db-persistence-20260722-1812`; execution guard `9ffd279b-9f2a-46ff-92b8-e81b9720709e`; wake guard `5e8611b4-48df-4201-8d4f-afcc7ae62bf0` (scheduled 2026-07-22 18:22 KST).
- Routing/source cards: service-open parent `82075744-b26a-4f0b-864a-22fb95359506`; planning blocker `dd69a30f-a817-4294-9e6b-fc51d8e430f5`; Kakao follow-through `7e9bffb1-9691-4c12-a2ab-c0283a7b7a62`. The routing artifact is `/workspace/tmp/room986-kakao-db-persistence-routing.json`; the follow-through's committed/local plan is `docs/plans/work-units/kakao-callback-continue-flow/03-contract-api/implementation-plan.md`. No local source-card body was found for the planning-blocker UUID, so its detail remains bounded to the routing artifact.
- Owner/flow: Backend/API Integrator owns the DB schema, migration, repository/service, and Kakao callback/additional-info persistence. The consuming mobile callback flow and existing `GET /auth/kakao/callback`, `POST /auth/kakao/continue`, and `POST /auth/kakao/additional-info` response shapes remain unchanged.
- Contract packet/baseline freeze: preserve the current dirty Kakao continuation work as a pre-existing baseline without editing its mobile or shared-contract paths in this slice. The stable fields are `kakaoDevAuthSuccessSchema` (`action`, `member`, `session`), `kakaoAdditionalInfoRequestSchema` (`continuationToken`, `email`, `displayName`, optional `phone`), `kakaoAuthContinueRequestSchema` (`outcomeId`), and the existing `blocked` action/reason/message shape. The direct `additional_info_required` JSON currently includes `kakaoUserId` while `kakaoAdditionalInfoRequiredSchema` and the mobile callback type do not model it; that known prior-slice drift is preserved, explicitly not widened by DB persistence, and must not be claimed resolved here. No `packages/contracts` or mobile change is planned for this slice.
- Persistence boundary: add `members` and `social_identities` DB tables. TypeScript fields remain camelCase and DB columns snake_case. Kakao identity lookup and member creation/update move behind an API service/repository; the route must not keep durable member, email, phone, or social-identity maps.
- Identity/uniqueness: a social identity is uniquely keyed by `(provider, provider_user_id)` and belongs to one member. Normalized non-null member email and phone are unique. A transaction must resolve existing Kakao identity, reject withdrawn members, reject cross-member duplicate email/phone, or atomically create member plus Kakao identity.
- Auth/session/retry/error behavior: preserve the existing dev-session success response and callback success status codes. On identity persistence failure, `GET /auth/kakao/callback` and `POST /auth/kakao/additional-info` fail closed with HTTP `503` and the existing blocked envelope `{ action: 'blocked', reason: 'AUTH_PERSISTENCE_UNAVAILABLE', message: <safe bounded message> }`; state-bound callback failures use the existing blocked redirect fields. `POST /auth/kakao/continue` has no DB access because its one-time result is created only after persistence succeeds, so its current behavior remains unchanged. A client may retry by restarting Kakao auth; no failed transaction yields a session. Existing short-lived OAuth state, additional-info continuation, and mobile auth outcome maps remain dev/sandbox ephemeral handoff state and are outside this durable identity slice.
- Tests/fixtures: first update `apps/api/src/routes/__tests__/kakao-auth.test.ts` with a DB-repository mock that survives route-module reload and proves signup becomes login after simulated process restart. Require focused red assertions for `(provider, provider_user_id)` uniqueness, normalized email and phone uniqueness/duplicate rejection, withdrawn-member rejection, atomic member/social-identity creation, and persistence-failure `503` mapping. Record the preflight dirty status/diff summary before the first red run.
- Expected paths: `apps/api/src/db/schema.ts`, generated `apps/api/drizzle/**`, `apps/api/src/services/kakao-identity.service.ts`, `apps/api/src/routes/kakao-auth.ts`, `apps/api/src/routes/__tests__/kakao-auth.test.ts`, and this work-unit/evidence directory. Mobile and `packages/contracts` are not expected to change for this slice.
- Migration/rollback: generate a non-interactive Drizzle schema diff. Rollback is forward-only removal after confirming no dependent data; no destructive rollback is executed in this task.
- Runtime smoke/service evidence: focused API test, API lint/build/typecheck, migration/schema validation, and `git diff --check` are required for checkpoint progress. `pnpm turbo run lint test` and `pnpm run test:runtime` are required before a PR-ready/Done claim; failures remain binding. No live OAuth is claimed. Dev deploy is optional only after all local and reviewer gates pass and safe existing CLI context needs no secret inspection.
- Evidence: `.evidence/room986-kakao-db-persistence-20260722-1812/`, including plan, red/green commands, checkpoint review, final reviews, diff summary, and status.
- Reviewers: `wm-contract-reviewer` and `wm-implementation-reviewer` at plan and final; contract reviewer at the material schema/repository checkpoint, with both reviewers included if route behavior materially shifts.
- Non-goals/human gates: no mobile UI/client change, Kakao console change, secret/env output, live OAuth/payment, production/release, destructive data action, dependency installation, push/merge/main, external proof claim, failed-gate acceptance, or 32-page HTML comparison.

## Checkpoints

1. Tests first: add the restart-persistence route test and focused schema/repository assertions for identity uniqueness, normalized duplicate email/phone rejection, withdrawn-member behavior, transaction atomicity, and safe persistence-failure mapping; run focused Kakao tests and record the expected failure before implementation.
2. Persistence implementation: add schema/migration and repository/service wiring; run focused tests plus API lint/build/type checks; inspect the checkpoint diff and obtain read-only contract review before proceeding.
3. Final verification: run applicable targeted/broader gates, inspect material `git diff` and full `git status --short`, record evidence, and obtain final `wm-contract-reviewer` and `wm-implementation-reviewer` verdicts.

## Source-of-truth inputs

- `AGENTS.md`, `REPO_OPERATIONS.md`, `PROJECT_ENVIRONMENT.md`.
- `.agents/skills/wm/SKILL.md` and `.agents/skills/mobile-backend-api-integrator-workflow/{SKILL.md,references/sot.md}`.
- `/workspace/skills/codex-role-workflow/SKILL.md`, `/workspace/skills/codex-interactive-repo-work/SKILL.md`, `/workspace/WORKFLOW.md`.
- `mobile-app-dev-team/runtime-sources/workflows/entry-case-routing.md`, `mobile-app-dev-team/governance/gates-and-evidence.md`, and `mobile-app-dev-team/runtime-sources/role-souls/backend-api-integrator-soul.md`.
- `/workspace/tmp/room986-kakao-db-persistence-routing.json` and the current Kakao follow-through plan referenced above.
